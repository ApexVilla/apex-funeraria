import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fenix.cliente.app',
  appName: 'Fênix Cliente',
  webDir: 'dist/client'
};

export default config;
